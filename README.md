# disease-prediction-system
A machine learning pipeline that predicts disease from patient-reported symptoms. Trains and benchmarks 5 classifiers, exposes why 100% accuracy on clean synthetic data is misleading, stress-tests models against injected real-world noise, tunes the most robust model (SVM) via GridSearchCV and deploys predictions through a desktop Tkinter interface.
